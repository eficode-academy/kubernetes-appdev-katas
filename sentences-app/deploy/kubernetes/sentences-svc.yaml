apiVersion: v1
kind: Service
metadata:
  labels:
    app: sentences
    component: main
  name: sentences
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: sentences
    component: main
  type: NodePort
