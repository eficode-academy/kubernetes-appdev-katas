apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: sentences
    component: name
  name: sentence-name
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sentences
      component: name
  template:
    metadata:
      labels:
        app: sentences
        component: name
    spec:
      #priorityClassName: medium-priority
      containers:
      - image: praqma/sentences-app:latest
        imagePullPolicy: IfNotPresent
        name: sentences
        args: ['--mode', 'name']
        resources:
          requests:
            cpu: 0.5
          limits:
            cpu: 0.5
