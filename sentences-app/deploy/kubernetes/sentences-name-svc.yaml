apiVersion: v1
kind: Service
metadata:
  labels:
    app: sentence-name
  name: sentence-name
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: sentence-name
  type: ClusterIP
